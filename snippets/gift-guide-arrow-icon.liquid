{% comment %}
  Shared arrow icon for the Gift Guide banner/popup CTA buttons.
  Inherits color from the parent via `currentColor` so it works inside
  both the base button state and the hover "fill" layer without duplication.
{% endcomment %}
<svg class="gift-cta__arrow" viewBox="0 0 24 10" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <path d="M0 5H23M23 5L18 1M23 5L18 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
</svg>
